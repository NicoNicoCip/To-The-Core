{
    "name": "origami-engine",
    "version": "0.1.0",
    "description": "A GMS 1.4-inspired game engine for TypeScript/Web",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm -r dev",
        "clean": "pnpm -r clean"
    },
    "devDependencies": {
        "typescript": "^5.3.3"
    },
    "engines": {
        "node": ">=18.0.0",
        "pnpm": ">=8.0.0"
    }
}